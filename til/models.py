# The TIL model was retired: its content was folded into blog Entry (see blog
# migration 0006_migrate_til_to_entry) and the til_til table is dropped by
# til migration 0006_delete_til. The app itself remains only to serve the
# /til/ -> blog 301 redirect shim (see til/views.py, til/urls.py).
